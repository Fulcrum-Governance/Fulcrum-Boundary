# Release Truth: v0.5.0 Post-Release Verification

Date: 2026-05-28

Status: pending tag and post-tag smoke.

This file is reserved for post-tag verification of the v0.5.0 Secure GitHub
live conformance preview package.

After `v0.5.0` is created, record:

- tag target commit;
- direct `@v0.5.0` install result;
- `boundary selftest` result;
- `boundary demo github-lethal-trifecta` result;
- `boundary command classify -- git push origin main` result;
- public proxy `@latest` result.
