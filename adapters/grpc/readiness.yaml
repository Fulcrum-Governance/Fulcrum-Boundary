adapter: grpc
status: preview
target_status: preview
lifecycle:
  parse: implemented
  identify: implemented
  evaluate: implemented
  deny: implemented
  forward: delegated
  inspect: implemented
  metadata: implemented
  record: delegated
  bypass_proof: delegated
  fail_closed: implemented
delegated_steps:
  - step: forward
    owner: gRPC server interceptor chain
    contract: docs/ADAPTER_CONTRACT.md
  - step: record
    owner: governance.AuditPublisher
    contract: docs/DECISION_RECORDS.md
  - step: bypass_proof
    owner: deployment topology
    contract: docs/security/FAIL_MODE_MATRIX.md
bypass_model: api-gateway
fail_closed_transports:
  - grpc
evidence:
  tests:
    - adapters/grpc/adapter_test.go
    - tests/adapter_conformance/grpc_conformance_test.go
  docs:
    - docs/adapters/GRPC.md
    - docs/ADAPTER_CONTRACT.md
    - docs/ADAPTER_READINESS_MATRIX.md
gaps:
  - id: BND-GRPC-001
    description: "Production status requires deployment bypass evidence; streaming workloads require per-message governance lifecycle tests."
    spec: "SPEC 2"
